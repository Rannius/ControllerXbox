[package]
name = "deck-version-helper"
version = "1.0.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
oozextract = "=0.5.5"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
